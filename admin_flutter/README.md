# DMS CONTROL Android target

Separate application ID and WebView storage. Loads `/admin/` on the same backend as DMS NEWS. Server role checks protect all administrative mutations. No universal credentials. Debug compilation and analysis passed in GitHub Actions; see `../docs/VERIFICATION.md` for exact emulator evidence and untested physical-device paths. Build instructions: `../docs/DEPLOYMENT-HINGLISH.md`.
