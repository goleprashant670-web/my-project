# DMS CONTROL Android target

Separate application ID and WebView storage. Loads `/admin/` on the same backend as DMS NEWS. Server role checks protect all administrative mutations. No universal credentials. Not compiled/device-tested here. Build instructions: `../docs/DEPLOYMENT-HINGLISH.md`.
